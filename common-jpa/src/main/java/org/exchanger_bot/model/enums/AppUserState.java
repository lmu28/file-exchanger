package org.exchanger_bot.model.enums;

public enum AppUserState { // состояния пользователя в зависимости от которых определяется, что он может делать(отправлять файлы / процесс работы с email )
    BASIC,
    WAIT_FOR_EMAIL
}
